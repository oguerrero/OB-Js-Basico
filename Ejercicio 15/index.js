const nombre = 'Oscar'
const apellido = 'Guerrero'

const datos = {
  nombre,
  apellido
}

// Session Storage
// sessionStorage.setItem('datos', JSON.stringify(datos))

// Local Storage
// localStorage.setItem('misDatos', JSON.stringify(datos))

// Coockie
// document.cookie = `datos = ${JSON.stringify(datos)}; max-age = 120`
